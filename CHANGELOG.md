# 更新日志

## 2026-09-25：AICNC 导入、库存恢复、生产与界面累积更新（V0.6.8，相对 `0ff651b`）

本次提交包含上次 GitHub 提交后的全部累积代码、测试、脚本和文档更新。以下按最终实现归纳；各阶段的历史验证和安装断点保留在交接记录及实施计划中。

### AICNC / Server 报表导入

- **新版优化目录**：增加独立的发现、预览、确认和忽略流程，按文件夹中的优化编号识别同一次优化，重命名或移动不重复导入；同时出现重复编号时提示整理。已确认或忽略的优化不再读取内部报表或比较文件版本。
- **材料分类与分配**：从大板统计和封边汇总读取材料，按工厂单选择正常优化或返工，再按订单、用途和 SKU 分配数量；分配总量必须平衡。正常优化追加材料并更新符合条件的工厂单优化状态。
- **返工领料**：返工独立生成材料出库单，再原子保存追加材料和等量消耗，保留原工厂单生产及出货事实；后续普通领料排除已扣减的返工量。不确定的外部结果先核对原单据，避免重复扣库存。
- **五金与旧流程隔离**：未出库工厂单按 SKU 和数量比较五金，相同内容跳过，不同内容由用户保留或替换自动五金，人工五金继续保留；已出库工厂单跳过五金读取。五金解析按列标题定位，兼容紧凑报表。旧预览不进入新版优化子目录，历史监控范围固定，退出后不重新加入。
- **历史目录误报**：排除已识别的旧来源，对未登记且不符合新版命名的历史目录按创建时间判断，不深入枚举；有效新版编号仍会发现，不因生成时间早而漏掉。旧监控范围清空后提示后续清理。
- **合批身份与中止订单**：合批板材表头只有在每个工厂单身份和订单归属均已确认时才复用中央库名称；共享目录预览及确认排除已中止订单，保留其历史事实。
- **显式迁移工具**：新增四张 AICNC 专用表的迁移入口，默认只读检查，显式应用前在线备份并核对原有业务表；普通 App 启动不创建这些表。此前已授权迁移的范围和证据见 [AICNC 实施记录](docs/plans/aicnc-folder-import.md)，本次 GitHub 推送不执行迁移或业务导入。

### 待处理中心与库存恢复

- 普通待处理项及 AIMES 异常使用当前连接的临时表，重开后由实际来源重新发现；保留旧持久表和历史数据。待处理队列为空时隐藏顶部入口。
- 库存操作的外部结果先持久保存，再提交本地业务事务；提交中、待核对、外部已确认或部分确认等未完成状态重新生成恢复入口。已在本地完整落地的历史操作不再误报为待恢复。
- 增加来源刷新、单项重新核对、外部单据核对和本地恢复入口。恢复前核对原计划、唯一备注、SKU、数量及单据号，未核清的订单阻止再次出库；不通过重试隐式重新扣减库存。
- 修复库存网页异步筛选未等待的问题，并校验实际打开的单据号、数量列和页面就绪状态；核对失败保留待办及可诊断说明。
- AIMES 启动恢复每日按需获取，当天已有成功记录时使用本地有效身份；用户手动刷新仍强制获取。跳过联网时明确提示本地数据，不声称已核对外部最新状态。

### 生产确认

- 生产面板在本次材料数量全零时提供“仅确认生产，不扣减材料”，只记录选中工厂单生产完成及时间，不新增材料消耗或调用库存；适用于此前已领料或使用余料的情况，无需新增数据库字段。
- 正数量保留原领料流程；继续拒绝非法数量、无效或未优化工厂单、重复生产、中止订单及库存待恢复等情况，并验证失败回滚。
- 修复生产预览被重复请求占用、导致全零确认按钮一直禁用的问题：弹窗统一发起一次读取，忙碌或失败明确回调并允许重读。

### macOS 界面与人工五金

- **人工五金布局**：新增表单固定在顶部，仅下方工厂单列表滚动；滚动区域与列表视口对齐。标题与订单号同排，顶部标题栏和底部操作栏统一为 **50 pt**，收紧表单留白，把高度留给列表。
- **五金搜索结果**：结果框左侧与表单标签对齐，新增的 98 pt 宽度分配给规格列；表头与内容共用列宽并水平居中，长规格自然换行。每页完整显示一项，上一项/下一项保留已选 SKU，修改查询重置页码及选择，避免嵌套滚动裁切内容。
- **只读工厂单**：已出库或其他不可编辑的工厂单保留“仅查看”，隐藏删除按钮及操作列标题；可编辑工厂单继续支持删除、撤销和统一保存。
- **看板与状态列**：调整助手订单卡片、订单号和阶段图标布局，减少重复显示与留白；工厂单四个状态列等宽，表头及图标文字组合居中。完成状态保留浅绿底，未完成状态使用浅灰文字和空心圆、无背景，长文本可悬停查看。

### 操作日志与诊断

- 设置页日志开关随常驻后台请求同步，在命令处理和缓存命中前生效；进行中的请求按开始时的设置完成记录。
- 日志查看增加失败筛选、订单/工厂单/操作内容搜索、刷新、展开及“复制本次诊断”。按会话和操作编号关联完整步骤，复制结果不受当前筛选限制；区分空日志、损坏行和读取失败。
- 未知异常仅记录类型和最多八帧代码位置，查看与复制经过字段白名单和脱敏；不输出异常原文、源码行或局部变量。启动日志补充版本信息，任务运行中拒绝清理日志，清理保留最近三个自然日。
- AIMES 进度去除“内容已省略”“网址参数已省略”等占位后缀，保留原脱敏边界；数据库动作日志与最终业务提交结果明确区分。

### 版本管理、维护与验证

- App 标题统一为 `PP FlowHub  Vx.y.z`。构建默认递增末位，较大功能可用 `--minor`；比较源码、已有构建包和已安装包的最高版本，打包检查成功后写回版本，安装同一产物不重复递增。
- 补充跨模块函数、接口与流程的中文说明，更新源码参考生成器、业务规则、数据模型、调用链、学习指南和实施记录；过时代码用途审计计划仍标记为进行中，不代表已完成清理。
- 最近一次完整发布门禁通过：Python 476 项（1 项原有跳过）、Swift UI、AIMES 离线表格、PP0067 workbook 端到端及差异检查；随后一次串行构建 V0.6.8 完成，Node / Playwright 打包检查通过。
- 本次推送前只读核验：安装目录已为 **V0.6.8**，Apple Development 签名完整校验通过；本次变更中随 App 打包的 43 个后端、工具及脚本文件与工作树逐字节一致。安装不是本次推送步骤执行的，签名与文件核验也不替代窗口交互验收。
- **仍待现场验证**：新版 AICNC 在真实 Server 恢复连接后的发现结果和速度、真实库存单据核对/恢复，以及最新人工五金界面的安装版交互验收。离线测试通过不等于上述外部流程已完成。
- 本次 GitHub 整理仅新增更新说明并核对提交范围、差异格式及远程状态，不重复测试、构建或安装；提交不包含真实数据库、凭据或构建产物。

## 2026-09-18：五金 SKU、生产事实、订单管理与界面累积更新（相对 `8a9e06a`）

### 业务功能与数据一致性

- **人工五金编辑**：新增按订单/工厂单维护人工五金的入口，支持搜索商品 SKU、批量新增、暂存删除与统一保存；保存前核对版本和工厂单可编辑状态，冲突或失败时保留可诊断结果。人工输入数量不执行报表中的滑轨折半规则。
- **五金 SKU 统一**：五金事实以 `product_code` 关联商品目录，名称、规格、单位从商品资料读取；移除重复显示字段和失效标记，保留 SKU、数量、归属及来源。已有五金引用的商品单位受到保护，后改映射或忽略规则不重绑已确认事实。
- **生产结构精简**：实际生产集中到 `production_records` / `production_materials`，工厂单直接关联生产记录；移除旧来源批次和冗余状态字段。升级前自动备份，迁移保留历史记录与业务时间，重复关联或校验失败时回滚；生产仍与工厂单出货分别记录。
- **材料事实保护**：完善已确认材料的 SKU、来源及分配数量校验，防止 Server 来源变化误删已确认事实；跨订单共享材料按完整分组核对及确认。调整成本、库存、订单详情和工作簿读取链，使其使用同一套持久事实。
- **订单中止**：增加“已中止”终态与二次确认入口；中止订单从默认未完成列表及助手进行中列表排除，但可筛选查回。后台生产和出库入口同步阻断，中止不删除历史、不退回库存，刷新和新工厂单同步不覆盖该状态。
- **AIMES 核验**：仍读取最近 50 条记录；对窗口外候选仅精确核验本地未完成生产的工厂单，按订单与工厂单双重身份识别，避免已完成生产事实被缺失结果错误停用。
- **Server 预览与确认**：映射保存后重新读取完整预览，在当前确认页继续处理；新增“确认无变化”，仅在校验完成、没有业务差异且本地事实未变化时推进文件监控基线。取消、预览或扫描不替代材料和五金的最终写入确认。

### macOS 界面与交互

- **材料确认预览**：宽度从 820 收至 720 pt，保持 650 pt 高度；五金编码、数量、单位列收紧为 100/56/40 pt，表头与内容共享列宽和 10 pt 间距，名称可换行；同步收紧材料变化表固定列。
- **主界面密度**：删除“界面设计说明”按钮及弹窗，待处理入口移到右侧末端；历史消息最小行高从 74 减到 59 pt，三行视口释放 45 pt 给订单列表，长消息仍可换行。
- **订单展开性能**：路径显示使用字符串解析，避免重绘时探测文件目录；列表展开/选择状态与消息区分离，消息区只响应相关输入变化；点击层不再嵌套 NSHostingView，保留单击展开和双击详情行为。
- **订单安排**：采用说明区和安排类型/开始日期/安装人三列表单；保留中文月历、安装人输入与历史选择。修复清除日期时访问已删除数组绑定导致的崩溃，并验证关闭后丢弃未保存草稿。
- **动作与成本**：生产、出货、中止按钮等宽且保持间距；计算成本仅在当前订单材料读取完成且存在正数量材料时启用，切换订单清空旧材料，迟到详情响应按订单身份过滤。清理不再适用的状态筛选项。
- **助手与消息状态**：拆单、优化、生产、出货连接线按各自完成数量独立显示，部分完成用绿色虚线、全部完成用绿色实线；修复“待处理中心”名称被误判为任务仍在运行的问题。

### 维护与验证

- 补充人工五金、五金 SKU、生产迁移、AIMES 核验、订单中止及 Swift UI 回归；同步业务规则、数据模型、学习索引、调用链、实施计划和交接记录。
- 加入 PP0072 材料恢复工具及记录：默认只读校验，应用需显式参数、备份、副本演练和身份/数量核对；推送工具不等于执行修复。
- 本次界面调整后完整发布门禁通过：Python 401 项（跳过 1 项）、Swift UI、AIMES 离线表格、PP0067 workbook 和差异格式检查。首次沙箱 Swift 宏插件受限，普通执行环境重跑成功。
- 一次串行构建与 Apple Development 正式签名安装成功，版本仍为 0.4.1 (5)。安装版已确认主界面按钮位置、消息区与订单列表布局；本次未执行真实业务确认写入。
- **未完成验收**：PP0064 现场材料预览等待超过两分钟仍处于读取阶段，尚未取得新预览窗口，长名称换行、五金列及底部按钮仍待实际视觉验证。离线门禁不代表全部外部系统流程已验证，细节见 `design-qa.md` 及各实施记录。
- 本次 GitHub 整理只新增更新说明并核对提交范围，不重复构建或将历史验证扩展为未执行的现场结论。

## 2026-09-09：GitHub 累积更新说明（相对 `ff62d8a`）

- **待处理中心**：统一四类处理状态，增加搜索、分类计数和类型专属详情；映射保存后续接 Server 预览，保留失败和未完成项，防止取消或过期回调串用订单上下文。
- **AIMES 与预览**：异常归属记录支持重开续办；复用已确认的工厂单身份，实时显示查询步骤和耗时，复用登录会话重试并限制查询总时长；预览失败显示实际错误，避免残留等待状态。
- **报表与五金**：同工厂单报表内容冲突时要求手工选择完整来源，内容变化使选择失效；单次预览共享报表清单和解析缓存，兼容旧版中文五金表头。
- **数据保护**：自动五金按工厂单原子替换并记录来源版本及前后审计，保留手工五金；隔离测试来源与正式库，防止材料专用预览清空五金、旧预览回退已确认出货状态。
- **生产领料**：即时保存传递生产上下文，最终事务统一提交生产批次，避免生产领料错误关联为工厂单出货。
- **界面**：助手统计居中并压缩留白，操作状态栏单行显示步骤及进度；订单安排使用固定底部按钮，保存成功后提示并关闭。
- **维护与回归**：补充历史五金恢复工具（默认只读计划，应用需备份与原出库单核对）、五金事实/旧版报表/来源选择测试，以及业务规则与交接记录。
- **验证范围**：各次历史发布验证见交接记录；本次整理提交仅更新说明并检查差异格式，不将历史测试结果视为当前完整快照的重新验收。

## 2026-09-09：工厂单归属复用、五金来源选择与预览进度

- 预览优先采用中央数据库中有效且已确认的工厂单归属，不再因名称缓存缺失而重复查询 AIMES。
- 同一工厂单五金报表内容不一致时必须手工选择，不按文件夹名称或时间判断；预览显示采用来源；确认写入后固定，仅在报表内容变化时提示保留或更新，决定随业务写入原子保存。
- 单次预览共享报表清单与解析结果，记录读取耗时和缓存命中；报表变化会使旧选择失效。
- AIMES 进度实时显示，页面查询先复用当前登录会话重试，整个查询限制为 90 秒；操作栏显示当前步骤与已用时间。

## 2026-09-09：Server 预览失败状态与重复读取修复

- 文件夹预览失败后显示实际错误及路径，清除残留的“正在预览”；错误文字包含“正在”也不会显示为运行中。
- 无效目录报表查找由两遍减为一遍，提示缺少的报表类型及订单目录选择建议。
- 单次预览复用已解析的五金数据，最终差异只生成一次；缓存不跨预览请求，保留确认写入门禁。

## 2026-09-04：助手统计与操作状态栏紧凑布局

- 三个订单统计区域整组居中，缩小高度与上下留白。
- 操作状态栏改为单行，运行时显示当前步骤并使用全宽进度条，结束后显示“操作完毕”。

## 2026-09-04：AIMES 格式异常支持重开后继续处理

- AIMES 销售单格式异常改为保存在独立 review 表中，不再因合法映射缓存排除异常行而导致确认归属时查不到原记录。
- 同日重开 App、AIMES 获取被正常跳过或暂时失败时，待处理中心仍保留尚未处理的异常；确认归属或忽略后才移除。
- 修正缺失 review 记录时误报“工厂单号不符合规则”的错误提示；人工确认不会改变已有出库状态。

## 2026-09-02：任务看板状态图标与状态链更新

- 任务看板的拆单、优化、生产、出货图标改为可组合的原生线性符号：拆单使用极简分支符号，优化使用堆叠方块符号，生产使用剪切加工符号，出货使用运输车辆符号。
- 移除旧的附件图标映射和圆形图标右下角额外完成徽标，避免图标样式不一致和出现绿色小点。
- 状态链保留最左侧短线位置；从拆单到出货的图标按 10pt、20pt、30pt、40pt 递进调整，连接线按相邻图标的新位置重新连接。
- 保持绿色/灰色状态语义、动态进行中虚线、完成线和未完成线的显示逻辑不变。
- 完成 macOS UI 回归测试、完整发布测试、App 构建、签名、安装和重启后的实际界面验证。

## 2026-08-31：订单中心表头位置微调

- 订单中心表头从“订单”到“出货进度”统一向右微调 `2pt`，更新时间和操作列保持原位；不改变订单数据、筛选、展开和操作逻辑。

## 2026-08-31：当前更新

本次更新以 `36e551c`（2026-08-28，`Improve operation timing and dashboard status reporting`）为对比基线，整理并提交当前工作区中的全部后续改动。

### 相对上一次更新的主要修改

- **助手工作台和订单看板**：把指令输入、当前操作、订单统计和进行中订单整合为更清晰的卡片布局；保留订单阶段连线轨道，补充拆单、优化、生产、出货的阶段状态、进度和时间信息；支持从助手看板直接打开对应订单。
- **订单状态事实和优化证据**：新增对 AICNC `nesting_result.xml` 的只读识别与留痕，按文件中明确的 AIMES 工厂单号记录优化证据；订单看板的优化完成时间和出货完成时间分别来自对应事实，不再用后续订单索引刷新时间代替业务完成时间；为已有数据库增加相应迁移字段和回填逻辑。
- **macOS 26 Liquid Glass 界面**：将应用页面、卡片、按钮和状态节点统一到 macOS 26 原生玻璃样式；调整默认窗口和页面间距；助手订单身份牌使用淡绿色背景/深绿色文字，完成、当前和未完成阶段使用不同但克制的状态色。
- **设置页**：重新组织为紧凑的两行两列配置卡片，移除手动“重新载入”入口；用户名和密码输入改为掩码；修正“保存全部配置”在常规设置、库存密码和 AIMES 密码分别成功或失败时的反馈口径。
- **日期、待办和订单详情**：统一订单安排、设置和待办的中文日期显示；新增/调整原生玻璃日历弹窗及尺寸；修复待办列表表头顶部圆角；Panel 多颜色值保持单行显示；订单安排安装人控件去除重复手绘下拉箭头。
- **生产和出货显示**：生产弹窗明确区分全部订单材料行数与有待分配材料行数，禁用剩余数量为 0 的材料输入；统一订单行“出货进度”的数量文本与进度条显示。
- **学习与接手文档**：新增全文件地图、Python/Swift/测试脚本全符号中文参考和用户操作业务调用链；增加可重复运行的 `tools/generate_code_reference.py`；同步修正 README、架构学习文档和接手指南中的现状说明。

### 变更边界

- Server 扫描仍不会写入材料或工厂单身份；只有被明确识别的 AICNC 优化证据可在扫描阶段记录，材料和工厂单事实仍需预览并逐单确认。
- 本日志只描述相对 `36e551c` 的代码和文档差异；更早版本的库存累计出库、浏览器保存回读等更新不在本次差异列表中。

### 2026-09-08 — 五金来源与出库进度保护

- 统一自动五金原子替换、来源版本审计与测试数据库隔离；修复材料专用预览可能清空自动五金的问题。
- 已确认出库不因当前资料差异或旧预览而回退；差异和来源完整性问题独立待核对。
- 核对原出库单后恢复 PP0072/PP0057 本地五金与出货进度，保留库存单、工厂单关联和手工五金。
