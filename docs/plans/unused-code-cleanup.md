# 过时代码与数据库用途审计

状态：审计与独立死代码清理进行中；数据库结构仅只读审查，尚未获准删除。

## 目标与约束

- 从当前 App、CLI、业务函数、测试和实际 SQLite 结构核对用途，清理无调用且无动态入口的实现。
- 保留任务开始时全部已有修改、业务记录、历史迁移、恢复工具、学习样例和设计参考。
- 空表、旧名称、只有异常时使用都不等于无用。旧 AICNC 监控是否可退役以持久监控范围为准。
- 删除真实表或字段前，提交准确结构差异、数据影响、备份和回滚方案，获得用户单独确认后才实施相关代码及迁移。

## 步骤与验证

1. 分别审查 Python、SwiftUI 和两个正式 SQLite 数据库；以当前文件为准核对引用和替代路径。
2. 清理高置信度的孤立函数与旧视图，更新学习说明并通过生成器刷新符号索引。
3. 运行最小相关回归；代码稳定后串行运行一次完整发布门禁、一次补丁构建、正式签名安装与对应入口检查。
4. 汇总保留项和待确认数据库方案，记录实际验证范围及工具阻碍。

本次修改前的源码/文档快照与工作树清单保存在 `/tmp/pp-flowhub-cleanup-20260925/`。该目录不是业务数据库备份；数据库尚未写入。

## 审计结论

待补充最终删除范围、保留理由及数据库方案。
