# 待处理中心实时检查与操作恢复

状态：代码、完整门禁和构建完成；正式签名安装及安装版窗口验收被工具限制阻塞。目标：普通问题仅保留当前会话检查结果；已产生外部影响的操作从持久操作记录恢复，并提供界面处理入口。

约束：保留工作树既有修改；不修改真实业务数据库结构，不删除历史问题表或记录；不以刷新重试外部写入。不执行真实库存扣减验收。

步骤：
1. 普通问题和 AIMES 待确认使用连接内临时表；按各来源检查节奏重新发现，历史问题不再投影。AIMES 启动仍遵守当天成功则跳过的频率，手动获取可强制刷新。
2. 待处理中心提供重新检查、AIMES 同步、映射及安全恢复入口；检查失败保留未知提示。
3. 使用 inventory_operations 的已有结构记录并核对外部结果，仅凭完整一致的单据恢复本地。
4. 隔离回归、完整门禁、一次串行构建、正式签名安装及安全入口验收。

验证：覆盖重启不恢复普通问题、旧记录不显示、外部修复后消失、失败不误清除、恢复幂等、单据不匹配拒绝及不重复出库。结果随后补充。

## 已完成与验证证据

- 普通问题与 AIMES 异常改为连接内 TEMP 检查结果；历史主库表保留，当前列表不读取历史问题。
- 待处理中心增加来源刷新、具体问题重新检查、库存单据核对恢复及打开库存系统入口；失败保留原因，各来源未知状态独立显示。
- 外部响应先保存操作恢复记录，再写本地事实。恢复使用原始计划，只读核对单据、备注、SKU 和数量；未核对操作阻止对应订单再次出库。
- 相关开发回归 209 项（1 项已有跳过）、新增恢复回归 9 项、补充生命周期回归 25 项通过。旧持久化测试调整为本次授权的新合同，同时保留同一会话的范围、回滚和资料错误断言。
- 最终 `./scripts/test-release` 通过：443 项 Python 测试（1 项原有跳过）、SwiftUI model/UI 回归、AIMES 离线页面、PP0067 workbook E2E、差异检查。日志 `/tmp/pending-release2.log`。第一次完整门禁的三项失败来自旧持久化测试前提，针对性修正后重新完成完整门禁。
- 一次串行 `./scripts/build-app --minor` 成功，版本 **0.5.0**，Bundle `com.pacificpride.ppflowhub`，产物 `/tmp/pp-flowhub-build/PP FlowHub.app`。可执行 SHA-256：`4b65524c984f85e9f48450e9cb2f08fc88cd47b7524af0cbb5d7e0b8a2b814a1`；日志 `/tmp/pending-build.log`。
- 包内七个 Python/JS 修改文件与工作树逐字节一致；使用打包后端及隔离库确认历史普通问题不显示、持久库存恢复项仍显示。
- 只读备份真实库到临时副本，在副本初始化后比较主库 schema 完全一致、历史问题数量不变、`integrity_check=ok`。未修改真实库结构或清除其历史记录。

## 剩余阻碍

电脑操作工具在选择 Terminal 时明确返回 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`，因此未控制 Terminal、未执行正式签名安装，未绕过限制或使用降级签名。构建产物已保留。下一步需通过普通 Terminal/Aqua 执行 `/Users/lantian/Documents/pp-flowhub/scripts/install-app`，再验收安装版待处理窗口。真实金蝶单据核对尚未现场验证；自动测试只使用隔离数据和只读页面替身，未执行真实出库。


## 0.5.0 后续修复：旧操作误报与核对读错页面

状态：修复、完整发布门禁及 0.5.1 构建完成；正式安装和现场单据验收仍受下述环境限制。

- PP0072 旧操作创建于 2026-08-21；本地 OFFICE `QTCK20260822002`、HALLWAY `QTCK20260822003` 的商品数量和关联工厂均能对应，后者有后续 `local_committed` 操作。新的只读投影已在真实库只读连接上验证：未收尾历史行仍为 1，当前库存待处理项为 0，`total_changes=0`，历史状态前后相同。
- 页面筛选修复 async predicate 误用 `Array.filter`，逐个等待判断；进一步核对目标页面单号及数量表头，拒绝其他单据和未加载完成的页面。核对读取错误与外部出库失败分开提示。
- 界面为历史库存问题提供“待核对”类别、原始日期与明确操作说明，成功提示采用后端实际结果，区分本地已完成与本次外部核对。
- 不改真实库结构、库存或业务记录；保留已有工作树。库存网站现场读取时跳转官网，尚未取得本次外部明细验收证据。

- 定向回归 96 项通过；完整门禁第二次通过：447 项 Python（1 项原有跳过）、SwiftUI、AIMES 离线页面、PP0067 workbook E2E 与差异检查。第一次完整门禁仅失败于旧的四分类 UI 断言，更新为包含“待核对”的五分类后重跑通过。日志 `/tmp/pending-fix-release2.log`。
- 一次串行补丁构建成功，版本 **0.5.1**。产物 `/tmp/pp-flowhub-build/PP FlowHub.app`，可执行 SHA-256 `ea46e65b5a521434cb57c1dfc3b2224b5d94785613809fcd1213badf0343dc9f`；日志 `/tmp/pending-fix-build.log`。包内 Python/JS 与当前源码一致；打包后端只读查询真实库返回零库存待处理项，写入数为零，原历史状态仍保留。
- 安装目录当前仍为 **0.5.0**；0.5.1 构建包未签名。沿用本会话已确认的 Terminal 工具拒绝，不绕过限制；尚未正式安装或验收新版窗口。需在普通 Terminal/Aqua 执行现有 `scripts/install-app` 后完成安装版验收。
