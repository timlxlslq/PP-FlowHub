# 操作日志诊断优化

状态：已完成（2026-09-23）。

目标：使设置中的日志开关对后续常驻后台请求生效；让失败记录包含安全的定位上下文，并能在 App 中筛选、搜索、展开和复制一次操作的诊断记录。

约束：保留既有工作树修改；不改变业务数据库结构、业务事实或外部系统；不增加依赖、日志平台、缓存或逐条 SQL 降噪机制；保留近三自然日清理规则。GPT-6 Sol 高推理强度负责产品代码，主代理负责规划、测试、审查与发布。

## 步骤与完成条件

1. 请求携带当前日志开关，后台在处理请求及缓存命中前更新日志配置；正在执行的任务不被终止。
2. 意外错误记录异常类型、有限调用栈和必要订单上下文；不记录局部变量、源码文本或敏感输入。启动记录包含版本信息。
3. 查看器支持失败筛选、搜索、详情、刷新与同一操作诊断复制；兼容旧日志，并区分空文件、读取失败和损坏行。显示与复制再次脱敏。
4. 隔离回归验证开关关→开→关、缓存命中、错误上下文及脱敏、旧日志读取和同操作复制范围；已有清理回归继续通过。
5. 审查最终差异，运行一次完整发布门禁、一次串行构建、正常 Terminal/Aqua Apple Development 签名安装，并检查安装版相关入口。

## 验证边界

测试仅使用临时日志、隔离数据库及 mock 外部服务。实际 App 验收限日志与设置入口；启动前确认不会隐式迁移真实业务库。完整门禁、构建、签名安装及窗口交互分别报告。

## 当前结果与剩余项

- 已核对当前日志实现、常驻服务、设置与查看入口，并保存本次修改前相关源码副本供差异审查。
- 代码已实现；主代理独立新增 Python/Swift 行为回归。25 项定向 Python 回归及 Swift UI 回归通过。
- 审查修正了 URL/特殊字符脱敏顺序、长输入先脱敏后截断、后台配置同步和协议错误关联边界。
- 首次完整门禁发现既有库存测试依赖已翻译的英文注释；将三个注释断言改成同一意图的代码条件检查，两项受影响测试通过。保留所有业务保护断言。另仅移除了门禁报告的既有纯空白行尾随空格。
- SwiftUI 宏在沙盒内无法启动；普通权限隔离测试通过。最终完整门禁通过：Python 428 项（缺少 Desktop CS004/PP0072 fixture 跳过 1 项）、Swift UI、AIMES 离线表格、PP0067 工作簿和差异检查均通过。日志：`/tmp/pp-flowhub-log-release-test.txt`。
- 桌面工具明确禁止控制 Terminal，未使用其他 UI 自动化绕过该限制。沙盒内签名查询返回 0，但经过审批的普通权限命令环境能读取有效 Apple Development identity；已通过该环境执行正式安装脚本，结果见下文。

## 发布与安装版验收

- 唯一一次 `./scripts/build-app` 成功；打包 Node v24.19.0 及 Playwright import 检查通过。版本 0.4.1 / build 5，Bundle Identifier `com.pacificpride.ppflowhub`。
- 通过有有效 identity 的普通权限命令环境执行绝对路径 `scripts/install-app`，完成 Apple Development 签名、helper probe、安装及归档。没有使用 Terminal UI 或替代签名；安装日志 `/tmp/pp-flowhub-log-install.txt`。
- 安装版可执行文件 SHA-256：`4a041dee2742ec6f1d9fbb86e5c7317ff00f5b203460894be1746a3338ce955f`。包内五个相关 Python 文件与当前源码逐字节一致；启动日志实证包含版本、构建号和日志开关。
- 已安装 App 实际点击：设置→查看→只看失败（显示十条历史错误）→详情（显示 database is locked 与 10.71 秒）→复制本次诊断（粘贴回搜索框验证包含同次开始、步骤及失败）→文字搜索（缩小为两条）→刷新（保留筛选并收起详情）。截图检查无明显遮挡。验证后退出 App；未启动 Terminal。
- 开关关→开→关及缓存命中、错误脱敏与旧日志兼容由隔离回归覆盖；没有在真实订单制造失败或执行生产、出库、确认写入。启动按既有流程执行了 Server 扫描并弹出待处理提示，选择稍后处理；未确认其业务变化。
- 本轮没有数据库结构变更。日志逐条 SQL 降噪、写入健康提示和自动轮转仍为后续范围。
