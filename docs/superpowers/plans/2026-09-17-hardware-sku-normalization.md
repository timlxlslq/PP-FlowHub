# 五金事实以 SKU 关联商品资料

- 目标：hardware_items 去掉 source_code/name/spec/unit，只保存 SKU、数量、归属、来源类型与必要定位；所有已映射五金显示商品资料，不反向还原报表。
- 状态：完成；代码、正式库迁移、完整门禁、构建、用户安装及安装版验收均已完成。
- 用户授权：修改代码及相关数据库；沿用真实库备份、副本演练、发布门禁与正式安装流程。
- 约束：保留既有工作树；保留左右滑轨一致性检查与单边数量、M1094–M1097 报表数量除二规则。转换只用于原始报表，不作用于已确认事实、人工输入或数据库迁移。新写入必须对应商品 SKU；确认后改 mapping/ignore 不改历史 SKU。
- 步骤：审计所有读写入口 → SKU schema 事务迁移及商品单位保护 → 详情/成本/Traveler/库存/预览读取统一 JOIN → 隔离回归 → 备份和演练 → 正式库迁移 → 全门禁、一次串行构建、正式安装、安装版验证。
- 验证：缺 SKU/禁用商品阻断并回滚；左右轨规则、偶数规则、重复确认及人工数量；已有 SKU/数量/归属逐行不变；其他业务表及出库历史不变；完整性/外键；商品资料显示及单位更新保护。
- 剩余决策：无。旧的来源修复命令不再从确认记录反查或重新映射；过往日志/备份保留原样，不新建报表历史表。

## 完成证据

- 384 项 Python 回归通过，1 项跳过；Swift UI、AIMES 离线回归、PP0067 workbook E2E 与完整发布门禁通过。一次串行构建完成，安装包内八个相关 Python 模块与源码逐字节一致。
- 备份及正式迁移证据：`data/database-backups/hardware-sku-20260917-093812/verification.json`。116 条五金的保留字段逐行相同；其他业务表不变；已有来源版本只更新规范指纹；完整性与外键通过。
- 17 个有五金的订单：旧/新副本详情业务字段、成本、库存需求一致（排除预期更新的商品显示属性与生成时间）。
- 用户在普通 Terminal 完成新版本安装；Apple Development 深度严格签名验证通过。版本 0.4.1，安装版可执行文件 SHA-256 为 `8a848c2bd57f97f44a5f310f5e079b3f5e2de8dec2a542a346bf8d57d6824d32`。
- 安装版 PP0064 显示商品名称 Unihopper Hinge、H-Rail/L-Rail 和正确数量；F2609170263 人工 M1068/M1069 各12 ea；回车搜索 M1094 显示商品规格 TS30305H、单位 Sets。验收后退出 App，再查五金事实未变。
- 未执行真实 Server 确认、业务保存或外部库存扣减。启动自动扫描仅更新发现信息。本任务此前 Terminal 控制及后台安装被工具/自动审批拒绝，本轮由用户安装，未绕过限制，也未操作用户其他 Terminal 窗口。
