# PP0072 历史材料恢复与来源清理保护

- 目标：恢复用户确认的六项材料，防止 Server 文件夹变化/文件缺失删除已确认材料。
- 状态：数量与 SKU 已核实，副本演练、正式恢复、完整发布门禁、一次构建、Apple Development 签名安装及安装版详情验收全部完成。
- 约束：只恢复 PP0072 material_items；不改生产、五金、出货、外部库存；保留已有工作树修改。
- 步骤：核对历史备份与生产消耗 → 修复清理边界并回归 → 在线备份、副本恢复演练、事务恢复 → 完整门禁、串行构建、Aqua 签名安装、安装版核验。
- 验证：空/部分/数量不同替代来源、相同来源去重、混单隔离、重复同步、文件删除；恢复前后其他全部表摘要不变，完整性与外键通过。
- 剩余决策：无。用户已确认 M1134 8 mm 3 张；外部材料单数量差异独立保留，不据此修改库存。

## 恢复范围

M0004=22、M0003=5、M0002=9、M0021=18、M1134=3 张；M0022=320 m。
参考 `data/database-backups/workflow-20260831-pre-ui-bundle.sqlite3`。
备份与审计：`data/database-backups/pp0072-material-recovery-20260916/`。

旧材料身份从备份映射到当前 products 已确认 SKU；恢复保留来源路径，以当前时间记录恢复写入，指纹使用现行 SKU 指纹规则。生产消耗与恢复需求相等，六项剩余量为零。新增 BATH 工厂单不继承旧工厂单生产状态。

## 发布结果

357 项 Python 回归（跳过 1 项）、macOS UI、AIMES 离线表格、PP0067 工作簿和差异检查通过。首次沙盒运行的 Swift 宏进程被拒绝，获准普通权限重跑后通过。Terminal UI 控制被工具禁止；未绕过该控制，获准直接运行正式安装脚本后 Apple Development 签名与安装校验成功。安装版详情六项数量正确，生产/出货 5/6，启动扫描后材料仍为六行。验收证据见恢复目录 release-verification.json。外部库存单当前数量未实时核实；历史倍量记录未修改。
