# Server 材料预览布局验收

- source visual truth: `/Users/lantian/.codex/generated_images/01a0ac6f-2ec1-7610-90f9-9c876a53f861/exec-ccc2a08c-51b9-4fba-9b5a-13f64f32334c.png`
- 用户精确修订：材料类型标题右移 10，数量变化标题左移 5；以代码中的 SwiftUI point 偏移执行，不采用生成图的不精确位移。
- implementation screenshot: 尚未取得新安装版截图。
- viewport: 正式 Sheet 为 820 × 650 pt；草图约 1410 × 1116 px。尚未进行密度归一化或同状态对照。
- state: PP0087 材料预览，工厂单展开；草图中的“示例工厂单”不进入正式产品。

## 验收边界

电脑控制工具拒绝访问 `com.apple.Terminal`，返回 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`。尚未完成普通 Aqua 会话中的 Apple Development 签名安装，因此不能使用旧安装版截图证明新布局。

字体与换行、间距与列对齐、配色与对比、SF Symbols 图标、完整文案均待新安装版截图验证。源码使用原生字体、现有 AppPalette 和原有背景；这些代码事实不代替视觉验收。完整视图和局部对照证据均未取得，不声明视觉通过。

## 待完成

1. 完成正式签名安装并重新启动安装版。
2. 打开 PP0087 的只读预览，对照选定草图，检查表头偏移、材料分列、数量对齐、长名称和底部按钮。
3. 验证工厂单展开/折叠；检查多工厂单与多订单的分组，不点击最终业务写入。
4. 截取同状态画面，完成视觉比较。

final result: blocked

---

# 订单人工五金编辑验收（2026-09-16）

- source visual truth: `/Users/lantian/.codex/generated_images/01a0aca1-305c-78c1-81c5-e10b36f65cff/exec-3d2be2c8-489c-4c6e-ad52-9c3412e1f4c7.png`。
- 用户确认范围：计算成本右侧增加人工五金按钮；按工厂单分组；SKU/名称搜索；新增、删除、统一保存；无备注。
- 实现：现有 SwiftUI App 的 `ManualHardwareSheet`，1040 × 760 pt，沿用 AppPalette、原生字体和 SF Symbols；不新建网页原型。
- implementation screenshot: 尚未取得新安装版截图。
- 当前阻碍：本任务调用 `cua.getApp("com.apple.Terminal")` 被明确拒绝，返回 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`。不绕过工具限制，不以旧安装版截图替代新功能验收。
- 待核对：正式安装后的按钮位置、工厂单分组、长名称/规格换行、列表滚动、商品检索和选择、数量输入、待删除撤销、取消放弃草稿、空态和保存错误态。真实业务数据不用于测试写入。

final result: blocked


## 人工五金安装后复核（2026-09-16，更新前述阻碍）

用户自行完成签名安装后，已通过电脑控制捕获实际界面。源草图和实际 PP0087 草稿/检索画面已在同一观察结果中共同输出对照。原草图 1487×1058，安装版 Sheet 1040×760；示例数据与真实订单不同，因此不宣称逐像素或相同业务数据复刻。

可见布局通过：按钮在计算成本右侧；标题、工厂单分组、SKU/名称/规格/数量单位/操作列清晰；无备注；新增区和底部固定取消/保存区域正常。单条检索时表单完整展示；8 条长名称结果可滚动，数量行位于外层滚动区；没有发现阻断操作的遮挡或溢出。保留原生 macOS Picker 和 Stepper，草图的示例文案没有进入产品。

交互通过：空态、SKU/名称搜索、选择商品、数量单位、暂存新增、放弃确认；PP0070 已保存记录读取和出库只读保护通过。真实数据库未保存测试草稿，PP0087 人工记录回查仍为 0。

范围限制：仅完成上述安装版可见状态与只读/草稿交互验收；真实保存、真实删除写库由隔离测试覆盖，本次没有执行。多工厂单满屏状态没有现场修改业务事实以造数验证。

final result: passed（上述布局与只读/草稿验收范围）

---

# 订单安排方案一与详情工具栏（2026-09-17）

- source visual truth: `/Users/lantian/.codex/generated_images/01a0b131-4cdb-7c12-896b-793eae94e258/exec-0681a508-0c6a-4edf-96be-766a86e26331.png`（用户选定的第一张生成图）。
- implementation screenshot: 尚未取得新安装版截图。
- viewport: SwiftUI sheet 最小 640、理想 700 pt 宽，自适应高度；源图 1521 × 1034 px。没有实施截图，无法做同密度归一化。
- state: 订单说明与两行安装安排；有日期、未填写、月历弹出和清除草稿待安装版检查。
- 字体：原生 SwiftUI 字体与中文回退；间距：说明区、三列表头、两行表单及细分隔线；配色：现有玻璃背景，中性日期框和蓝色保存；图标：SF Symbols；文案：沿用真实字段，无虚构状态。这些仅为代码证据，不代表视觉验收通过。
- 完整视图、按钮边缘间距与日期/安装人局部对照均未完成。工具明确拒绝控制 Terminal，正式 Aqua 签名安装和随后截图受阻；不使用旧版截图代替。
- 待安装后验证：未填写时打开月历、中文日期、安装人手输及历史下拉、清除后重新选择、关闭丢弃草稿；仅在隔离环境测试保存。详情五个按钮可见边缘应等距，工具栏至表格间距减少 5 pt。

final result: blocked

## 安装版验收续记（2026-09-17）

用户完成安装后，普通执行环境 `codesign --verify --deep --strict` 通过，Authority 为 Apple Development，TeamIdentifier 为 ZF64PZKWMD。安装版可执行 SHA-256 为 `7710df91bcef0220f9551982020dfe2b2aa7f02a683ce6bd872c0b6a8011c57f`（签名后哈希变化）。

Computer Use 本会话已返回 640 × 510 的订单安排窗口截图：三列标题、分隔线、中性日期框、中文日期、安装人输入和蓝色保存按钮可见；截图只保留在会话工具结果，尚无独立文件和同密度组合对照。实际操作确认选择日期可弹出月历并改为 2026年9月18日，安装人可手输，历史下拉选“小齐”可回填。均为 PP0064 未保存草稿。

[P1] 点击清除日期导致 App 崩溃。证据：`~/Library/Logs/DiagnosticReports/PPFlowHub-2026-09-17-154813.ips`，调用链为 `Array._checkSubscript` → `Binding.subscript.getter` → `OrderAnnotationsEditor.installationRows` 删除按钮。源码在删除数组元素之后再次读 `day.id`。修复为删除前捕获 `removedID`，先关闭对应月历再删除。修复后安装版复验、关闭丢弃草稿、详情五按钮间距和工具栏高度仍待完成；不能宣称完整视觉验收通过。

final result: blocked

## 修复版安装后复验完成（2026-09-17）

- 安装版签名严格验证通过，Apple Development / ZF64PZKWMD，签名时间 15:52:16；可执行 SHA-256 `2726ffd6a57afbe0b8dee7583594fb06de703a4e3d7caa3869d43243c0407a69`。已先退出旧进程，再启动新安装版。
- 实施截图：`/tmp/pp-flowhub-arrangement-verified.png`（640 × 510）；工具栏：`/tmp/pp-flowhub-toolbar-verified.png`（1075 × 768）。源图仍为上文第一张方案图（1521 × 1034）。两图在同次图像检查中打开对照，未拉伸；这是原生窗口布局验收，不声称逐像素一致。
- 完整视图：标题、订单号、上方说明、三列表头及两行表单、右下关闭/保存保持方案一层级。原生 640 pt 窗口沿用系统字号与自适应高度，画面比例不同于概念图；文字清楚且无裁切。
- 局部：中性日期输入、日历符号、分隔线、蓝色保存符合方向；未选择日期时显示“选择日期后填写安装人”，有日期后才出现输入与清除按钮，沿用现有业务交互，不创建空日期数据。工具栏五按钮背景等宽、边缘间距一致，禁用生产/出货状态保留；源码间距 12 → 7 pt 已核对，未把截图缩放后的像素差当作逻辑点。
- 字体/配色/素材/文案：系统中文字体和原生 SF Symbols 清晰，现有浅色玻璃背景延续参考，保存操作突出；未引入位图或虚构业务状态。概念图与原生系统控件的字号及比例差异为可接受适配。
- P1 复验：计划日期新增 → 收起月历 → 清除后回到“选择日期”；实际日期同样新增和清除，窗口保持正常，均未崩溃。先前 P1 已解决。
- 关闭复验：填写“未保存的界面验收草稿”后关闭，再打开 PP0064，说明和两种日期均为空，未保存草稿已丢弃。没有点击保存或执行真实业务确认；真实保存不在本次安装版验收范围。
- 验收后已退出 App。本次没有启动 Terminal。

final result: passed

---

## 材料预览收窄与主界面密度（2026-09-18）

- source visual truth：本次用户附图 `PP FlowHub Appshot 2026-09-18T22-40-43.606Z.png`；要求在现有原生界面上收窄预览、收紧列、删除说明按钮、历史行减少 15 pt。
- implementation screenshot：本会话 Computer Use 的安装版主窗口截图（1075 × 768）；未落地独立截图文件。正式 App 0.4.1 (5)，SHA-256 `c2c0055e79b35018455ff3c91e26ce248bd58559dd10cc25e4b686c21727fbb6`。
- viewport：用户附图与当前工具截图缩放不同，不作物理像素精确比较；源码预览 720 × 650 pt，历史视口 177 pt。
- 实际主窗口：右侧仅保留待处理按钮，打开待处理中心成功；历史显示三条，订单列表可显示到 PP0062。字体、语义色、SF Symbols、玻璃背景和正文可读性保持正常，未发现主窗口裁切。
- 材料预览状态：选择 PP0064 并只读预览，等待超过 127 秒仍为“正在读取材料并核对数据库中的工厂单归属”。未得到新预览窗口，无法完成同状态完整及局部对照，不能宣布五金列、长名称、底部按钮的视觉 QA 通过。
- 未执行映射、忽略或最终写入。未将源码列宽和测试通过替代材料预览视觉证据。
- 发布：完整门禁通过（401 Python，跳过 1；Swift UI、AIMES offline、PP0067 workbook）；一次串行构建和 Apple Development 正式安装通过。Terminal UI 被工具禁止，但普通执行环境存在有效签名身份，现有安装脚本直接执行成功。
- 待验证：现场预览返回后检查 720 pt 窗口内名称换行、编码/数量/单位列对齐、底部按钮及工厂单折叠展开。

final result: blocked

---

## 工厂单状态标签（2026-09-21）

- source visual truth：`/Users/lantian/.codex/generated_images/01a0c794-9f83-7ee3-a83c-5fae1d302b56/exec-497e6e32-9bfc-474f-9bcb-002e2658a785.png`，1680 × 936 px 概念图；用户追加要求：未完成文字再浅一级，列标题与列内容均居中。
- implementation screenshot：尚未取得修改后安装版截图；不以旧 App 截图充当新样式证据。
- viewport / density：目标为现有原生窗口与固定列宽；概念图为放大示意，未进行密度归一化或逐像素比对。
- state：工厂单展开，需包含已优化/待优化、已生产/未生产、已出库/未出库，并检查选中行及长名称。
- 字体：源码状态文字 13 pt medium、单行截断；实际尺寸可读性待验。
- 布局：源码标签 24 pt 高，圆角 6 pt；标题与内容共用固定列宽，整体居中；实际截图对齐待验。
- 颜色：完成沿用绿色语义，未完成文字 RGB 0.57 / 0.60 / 0.65，比前图更浅；未声明可访问性对比度达标。
- 图标：原生 SF Symbols `checkmark.circle.fill` / `circle`，没有引入位图；渲染待验。
- 文案：沿用真实状态和出库单据文本，长状态可悬停查看；待校验、未查询不改名。
- full-view / focused comparison：均待新安装版截图，无已通过的视觉对照。
- 阻碍：本次 `cua.getApp('com.apple.Terminal')` 明确返回 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`，未绕过工具限制，未完成签名安装。
- 待完成：普通 Terminal 运行 install-app，重启安装版，检查上述状态和选择交互，完成全图及局部对照后退出 App。

final result: blocked

### 工厂单状态标签后续修订（2026-09-21）

用户最新要求覆盖初版：四个状态列等宽等距，未完成无浅灰背景，状态文字恢复原大小。源码已改为四列各 127.5 pt、状态文字 `.caption`、未完成 `Color.clear`，标题与内容仍共用列宽并居中。完成浅绿标签和浅灰未完成文字保留。视觉目标为上文方案图叠加这三条明确修订，不要求放大图的 13 pt 字号或灰底。安装版截图及完整/局部对照尚缺，前述 Terminal 工具限制尚未解除，不能声称安装验收通过。

final result: blocked


---

## 助手订单卡片材质与阶段轨道（2026-09-21）

- source visual truth：`/Users/lantian/.codex/generated_images/01a0c7a6-08d9-7770-81c7-373a25b200c2/exec-29944b00-e623-462a-a747-ae660d7c0aa9.png`；用户确认柔和卡片、独立订单号、圆角方形图标底座、细实线/虚线及图标上下标签，并授权在实际 App 修改。
- implementation screenshot：未取得新安装版截图。旧实际截图与 AI 效果图不作为实现通过证据。
- viewport / density：源图 2172×724 px，是单卡片放大设计；实际目标为现有固定 macOS 窗口内的订单行。未取得实现截图，因此未进行裁剪、密度归一化或逐像素对照。
- state：待验证 PP0064 的四个独立阶段、灰色未开始阶段、长订单号、双击订单跳转和纵向滚动；未点击任何真实业务确认操作。
- 字体：实现订单号 26 pt semibold、阶段名 15 pt semibold、数量 14 pt 等宽数字，保留长编号缩小和悬停提示；实际字体观感和截断待验。
- 间距：身份区 176 pt，分隔线 78 pt；轨道四等分，节点 56×56 pt、圆角 16 pt，名称/数量与节点居中，节点中心 y=52 pt；整体布局、首屏密度和最右节点裁切待验。
- 颜色：浅色 material、白色至薄荷色覆盖、细高光边缘及轻阴影；阶段用低饱和绿色，未开始为灰色。未测量实际对比度。
- 图标：原生 SF Symbols `arrow.triangle.branch` / `square.3.layers.3d` / `scissors` / `truck.box`，28 pt medium 单色；未使用位图替代 UI。渲染与视觉重量待验。
- 文案与事实：保留真实订单号、阶段名称与完成数量；`assistantProgressSegmentState` 不变。部分完成绿色流动虚线（7/9 pt），完成实线，未开始灰线；减少动态效果仍暂停流动。
- full-view / focused comparison：因安装被阻止，两类实际对照均未完成；没有作出视觉通过结论。
- 自动验证：首次沙盒执行被 SwiftUI StateMacro 插件权限阻断；普通环境验证发现旧胶囊身份牌断言尚未更新，按用户确认的新设计更新视觉断言后界面专项通过。最终完整 `scripts/test-release` 通过：422 Python 测试，跳过 1；macOS UI、AIMES 离线表格、PP0067 workbook、diff 检查均通过。日志 `/tmp/pp-flowhub-dashboard-release-final.log`。
- 构建：一次串行 `scripts/build-app` 通过，Node v24.19.0；`/tmp/pp-flowhub-build/PP FlowHub.app`，Bundle `com.pacificpride.ppflowhub`，版本 0.4.1 (5)，可执行 mtime 2026-09-21 23:16:02 PDT，SHA-256 `4a97da980f940e445436fb279517f0b34705fed5d070cd7e560ae6965122e969`。日志 `/tmp/pp-flowhub-dashboard-build.log`。
- 数据边界：本次仅 SwiftUI、对应视觉断言和文档变更；构建前 Python 顶层模块逐文件与已安装 App 一致，未引入数据库迁移。门禁使用隔离夹具，不作为真实外部服务验证。
- 阻碍：本次 `cua.getApp("com.apple.Terminal")` 明确返回 `Computer Use is not allowed to use the app 'com.apple.Terminal' for safety reasons.`。未尝试其他方式绕过，未执行签名安装，未启动未签名 App 代替正式安装版。
- 待完成：在普通 Terminal 运行 `/Users/lantian/Documents/pp-flowhub/scripts/install-app`，退出旧 App 后打开安装版，完成同状态全图/局部对照、长编号和双击/滚动检查，再按项目要求退出验收 App 和本次安装 Terminal。
- 工作树：保留本任务开始前已有的订单中心状态标签代码与记录；本次未覆盖这些改动。收尾时另外出现 `agent_runner.py`、`backup.py`、`runtime_store.py` 的并行修改，未触碰这些文件。逐文件验证本次构建中的这三个模块仍与已安装 App 一致，尚未包含后来的修改；本次门禁结论仅覆盖受测与已构建版本，不能用于这些后续后台修改。

final result: blocked

### 订单号居中与卡片高度精简（2026-09-22）

用户追加要求覆盖前图：去掉“订单”小标题，编号在左侧区域居中；移除阶段图标下方数量并收紧高度。源码现为订单号在 176 pt 区域居中，轨道 80 pt、上下各 16 pt，卡片总高 112 pt（原 138 pt），分隔线 62 pt。阶段名、56 pt 图标、节点中心 y=52 及各阶段独立判断不变；数量仍用于事实判定和辅助阅读标签，不绘制在节点下方。

本次 `test-release` 中 422 Python 测试（跳过 1）、macOS UI、AIMES 离线、PP0067 workbook 均通过；最后全工作树 diff 检查因并行修改中的 `traveler_assistant/core.py` 八处行尾空格失败，故完整门禁未通过。未改动其他任务的 Python 文件；本任务四个文件 diff 检查通过。日志 `/tmp/pp-flowhub-dashboard-compact-release.log`。

一次串行 `build-app` 成功，产物 `/tmp/pp-flowhub-build/PP FlowHub.app`，日志 `/tmp/pp-flowhub-dashboard-compact-build.log`。构建包含当时工作树，其他任务仍在修改业务源码，测试与构建不代表后续修改已验证。前次工具明确禁止控制 Terminal 的限制未解除；本次未绕过限制或重复尝试，未签名安装、未启动新包。安装版实际截图、完整/局部视觉对照和操作验收仍待完成。

final result: blocked

### 人工五金标题、底栏与滚动范围（2026-09-25）

- 目标来源：`/var/folders/80/v0wn4lyn4075k3fv5v2chmdw0000gn/T/codex-clipboard-cf2cca1a-da18-4ea4-ae93-5866321b2a87.png`，1648×1302 像素，以及用户三条标注；弹窗逻辑尺寸 820×620 点。
- 修改前证据：本次通过电脑操作工具查看已安装 V0.6.2 的 PP0064 人工五金弹窗；空搜索、无草稿、列表在顶部。当前截图确认双行标题、较高底栏，以及轨道起点高于首个工厂单卡片。
- 已实施：标题与订单号按文字基线同行；顶部/底部纵向内边距从 20 点改为 10 点；列表内部移除纵向内边距，上下间距放到滚动区外侧（14 / 8 点）。未更改卡片、字段、按钮尺寸、分页或业务行为。
- 字体与文案：沿用系统字体、原字号字重及全部文案，只改变订单号位置；新版实际换行仍待验收。
- 间距与布局：上述源码尺寸已核对，窗口总大小保持 820×620；新版轨道上下对齐、列表增高及滚动固定区域仍待实际截图确认。
- 颜色与视觉样式：沿用 AppPalette 及按钮样式；未新增颜色或透明度定义。
- 图像与图标：无新增栅格资源，沿用现有 SF Symbols。
- 实现截图路径：未取得新版安装后截图。V0.6.2 修改前截图不能作为本次变更的完成证据；未进行新版全图/局部联合对照或像素归一化比较。
- 阻碍：此前本会话电脑操作工具明确拒绝访问 `com.apple.Terminal`，安全限制没有解除证据；按项目规则不绕过、不启动未签名包替代正式安装验收。需安装本轮产物后，再捕获同状态窗口并比较标题、底栏及滚动轨道。
- 待完成：正式签名安装；空搜索和长规格搜索两种状态截图；列表滚动时顶部表单和底部按钮固定；无草稿取消，不保存真实业务修改。

final result: blocked

本轮自动门禁通过（476 项 Python，1 项原有跳过；Swift UI、AIMES 离线、workbook、差异检查通过），V0.6.4 已构建；这些证据不替代新版截图对照。安装目录仍为 V0.6.2。

final result: blocked

### 新增人工五金卡片底部视觉留白（2026-09-25）

- 目标来源：`/var/folders/80/v0wn4lyn4075k3fv5v2chmdw0000gn/T/codex-clipboard-9ba9b574-c0b9-4081-ba77-019465ca40dd.png`，1648×1302 像素；用户要求减少数量行下方留白，使卡片上下视觉更平衡。
- 修改前证据：现场已安装 V0.6.4，PP0064 人工五金、空搜索、无草稿。电脑操作截图确认底部可见空白大于标题上方；列表滚动位置与用户截图不同，不将两张图视为同状态像素对照。
- 原因与修改：外层原本上下各 16 点，但底行按钮有 44 点最小布局框，控件轮廓外还有留白。本次仅将新增卡片底部 padding 改为 8 点；顶部和两侧保持 16，按钮及输入控件尺寸不变。
- 五项视觉检查范围：字体/字重/文案、颜色/AppPalette、SF Symbols 图标均沿用；唯一变更是卡片底部间距。新版字号换行、颜色、图标和实际上下空白仍待安装后截图对照，不以源码推定视觉验收通过。
- 新版实现截图：尚未取得；窗口仍为 820×620 点。缺少新版截图，未做全图或局部联合对照，也未进行密度归一化比较。
- 阻碍：本会话此前明确禁止通过电脑操作工具访问 Terminal；不绕过该限制，不以未签名包代替正式安装版验收。构建后需在普通 Terminal 正式安装，再对照卡片顶/底两处空白。

final result: blocked

本轮完整门禁通过（476 Python，1 原有跳过；Swift UI、AIMES 离线、workbook、diff 通过），已构建 V0.6.5，安装版仍为 V0.6.4。新版截图对照待完成。

final result: blocked

### 人工五金标题栏与底部操作栏等高（2026-09-25）

- 目标：用户最新红色标注明确指向整个弹窗的标题栏与底部操作栏；此前将问题理解为新增卡片内部留白，未解决本项。
- 视觉来源：本轮附带的红色标注图；未标注原图路径 `/var/folders/80/v0wn4lyn4075k3fv5v2chmdw0000gn/T/codex-clipboard-4e44c9f7-35b1-441e-80f6-2121aaf832e0.png`。状态为 PP0064、无搜索结果、列表接近底部；弹窗 820×620 点。
- 已实施：标题栏与底部操作栏共享 `AppLayout.controlHeight`（44 点）总高度；取消两栏额外纵向 padding，内容垂直居中，保留 20 点水平边距。原底栏按钮最小高度 44 点再叠加上下 10 点，导致底栏明显比标题栏高。
- 检查范围：仅两栏总高度与垂直留白改变；字体字重、文案、颜色、SF Symbols 图标和按钮样式沿用。新增卡片及列表不作额外修改。
- 尚缺新版安装截图，不能完成全图及标题/底栏联合局部对照或密度归一化比较；源码统一高度不等于实际窗口视觉验收通过。
- 阻碍：本会话 Terminal 电脑操作工具此前明确被安全限制拒绝，没有解除证据；需正式安装本轮产物后，检查两栏从外缘至分隔线的总高度及控件居中、可见性和点击状态。

final result: blocked

本轮完整门禁通过，V0.6.6 构建成功；当前安装版 V0.6.5。这些证据不替代新版两栏等高的截图检查。

final result: blocked

### 标题栏与操作栏按用户指定改为 50 点（2026-09-25）

用户明确指定两栏统一为 50 点，覆盖上一轮 44 点方案。两栏现共用 `ManualHardwareSheet.barHeight = 50`，内容仍居中，20 点水平边距保持原样；全局按钮高度没有改变。本次仅调整这一参数及当前说明。新版安装后需核对两栏高度与控件显示；此前 Terminal 安全限制没有解除证据，尚无本轮安装版截图。

final result: blocked

50 点版本完整门禁通过，V0.6.7 已构建；安装目录仍为 V0.6.6，尚待正式安装与窗口视觉验收。

final result: blocked

### 只读工厂单不显示人工五金删除入口（2026-09-25）

用户截图来源：`/var/folders/80/v0wn4lyn4075k3fv5v2chmdw0000gn/T/codex-clipboard-e9f00435-c832-4059-b791-237c663dc3ee.png`。PP0064-ADU KITCHEN 已显示“仅查看”，各行仍显示红色删除文案。本轮沿用后端 `editable` 权限，只读分组不生成删除按钮，也不显示“操作”标题；保留 44 点空列，使其他列与可编辑分组对齐。忙碌时可编辑分组仍保留禁用入口；保存层锁定规则不变。

字体、卡片尺寸、颜色定义与其他文案不变，移除只读红色操作提示。新版安装后的实际可见性及无障碍按钮状态未验收：本会话 Terminal 工具安全限制仍无解除证据，正式安装及截图对照待完成。

final result: blocked

只读删除入口修改完整门禁通过，V0.6.8 已构建，安装版仍为 V0.6.7。新版截图和实际按钮可见性验收待完成。

final result: blocked
